# Fixed lint baseline for this repository.
#
# Strategy:
# - Start with package:lints/recommended.yaml for stability.
# - Add stricter rules that are compatible with Dart 3.1.5.
# - When SDK is upgraded, expand this file toward "all lint rules".

include: package:lints/recommended.yaml

linter:
  rules:
    always_declare_return_types: true
    always_put_required_named_parameters_first: true
    avoid_empty_else: true
    avoid_redundant_argument_values: true
    avoid_types_on_closure_parameters: true
    directives_ordering: true
    prefer_expression_function_bodies: true
    prefer_final_locals: true
    sort_child_properties_last: true
    unawaited_futures: true
